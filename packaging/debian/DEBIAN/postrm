#!/bin/bash
set -e

# Post-removal script for family-policy

# Reload systemd if available
if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload || true
fi

# Note: We don't remove /etc/family-policy or /var/lib/browser-extension-policy
# automatically to preserve user configuration and state

exit 0
